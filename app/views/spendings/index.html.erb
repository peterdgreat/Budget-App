<div class="d-height d-flex flex-column justify-content-between" >

<%= render partial: "shared/navbar", locals: { title: "TRANSACTIONS" } %>
<br/>
  <% if @spendings.blank?%>
    <p class="text-center ">You don't have any Spendings yet! Click below to add one.</p>
  <% else %>
<div id="spendings"  class="mt-5 ">
  <% @spendings.each do |spending| %>
    <%= render spending %>
  <% end %>
</div>
<% end %>
<section class= "end">
<div class = "new-group d-flex justify-content-center">
<%= link_to "New Spending", new_spending_path,  class: "btn btn-outline-light"%>
</div>
<section>

</div>